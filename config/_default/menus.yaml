# Menú principal
#   Los enlaces con `/#id` apuntan a los bloques de la portada (campo `id` en content/_index.md).
#   `weight` define el orden.

main:
  - name: Inicio
    url: /
    weight: 10
  - name: Blog
    url: /blog/
    weight: 20
  - name: Etiquetas
    url: /tags/
    weight: 30
  - name: Contacto
    url: /#contacto
    weight: 40
